<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= full_title(yield(:title)) %></title>
    <meta name="author" content="duende84">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <!--[if IE 9 ]>
      <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body>
    <header>
      <%= image_tag("/assets/logo2.png", :alt => "TendenciaZero", :class => "logo")%>
      <% if signed_in? %>
        <%= link_to "Cerrar Sesión", signout_path, method: "delete", :id => 'btn_logout', :class => 'button2 rounded gold' %>
      <% else %>
        <%= link_to "Iniciar Sesión", signin_path, :id => 'btn_login', :class => 'button2 rounded gold' %>
      <% end %>
    </header>
    <%= render 'layouts/messages' %>
    <%= yield %>
  </body>
</html>